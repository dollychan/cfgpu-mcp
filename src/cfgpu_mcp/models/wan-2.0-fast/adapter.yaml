# Inherits all config from wan-2.0; only differences listed here
extends: wan-2.0

adapter_id: wan-2.0-fast
display_name: "WAN 2.0 Fast (Seedance 2.0 fast)"
cfgpu_model_id: wan-video-fast
cost_tier: 2
speed_tier: 4

poll_config:
  default_timeout: 300
