adapter_id: seed-tts-2-0
display_name: "豆包语音合成 2.0 (Seed-TTS 2.0)"
cfgpu_model_id: seed-tts-2.0
model_name: seed-tts-2.0

task_type: audio
endpoint: /voice/generations
is_async: true
poll_endpoint: /voice/tasks/{task_id}

capabilities:
  - text_to_speech

cost_tier: 3
speed_tier: 3

poll_config:
  base_interval: 3
  max_interval: 15
  backoff_factor: 1.3
  default_timeout: 300
